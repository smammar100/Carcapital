import { ComingSoon } from "@/components/shared/coming-soon";

export default function SalesPipelinePage() {
  return (
    <ComingSoon
      title="Sales Pipeline"
      description="8-column kanban (New Lead → Completed Sale + Lost). Lands in Step 7."
    />
  );
}
