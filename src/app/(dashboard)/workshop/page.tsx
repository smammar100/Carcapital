import { ComingSoon } from "@/components/shared/coming-soon";

export default function WorkshopPage() {
  return (
    <ComingSoon
      title="Workshop"
      description="External walk-in service jobs (different from internal stock prep). Lands in Step 5."
    />
  );
}
