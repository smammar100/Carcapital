import { ComingSoon } from "@/components/shared/coming-soon";

export default function ActivityLogPage() {
  return (
    <ComingSoon
      title="Activity Log"
      description="Filterable timeline of every create / update / delete across the platform. Lands in Step 10."
    />
  );
}
