import { ComingSoon } from "@/components/shared/coming-soon";

export default function VendorsPage() {
  return (
    <ComingSoon
      title="Vendors"
      description="Vendor CRUD with active jobs count and total spent. Lands in Step 9."
    />
  );
}
