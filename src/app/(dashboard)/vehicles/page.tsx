import { ComingSoon } from "@/components/shared/coming-soon";

export default function VehiclesPage() {
  return (
    <ComingSoon
      title="All Vehicles"
      description="Inventory list with filters, table + card views, CSV export. Lands in Step 4."
    />
  );
}
