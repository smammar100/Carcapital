import { ComingSoon } from "@/components/shared/coming-soon";

export default function InvoicingPage() {
  return (
    <ComingSoon
      title="Invoicing"
      description="Purchase + Sales invoices, View / Email / Print, upload, VAT summary. Lands in Step 8."
    />
  );
}
