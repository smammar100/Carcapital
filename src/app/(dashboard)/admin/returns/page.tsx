import { ComingSoon } from "@/components/shared/coming-soon";

export default function ReturnsPage() {
  return (
    <ComingSoon
      title="Vehicle Returns"
      description="Return processing with vendor / supplier / g-trader / other resolution paths. Lands in Step 9."
    />
  );
}
