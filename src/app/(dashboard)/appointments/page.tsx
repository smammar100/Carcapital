import { ComingSoon } from "@/components/shared/coming-soon";

export default function AppointmentsPage() {
  return (
    <ComingSoon
      title="Appointments"
      description="Calendar + list view with booking form (WhatsApp / email mock notifications). Lands in Step 7."
    />
  );
}
