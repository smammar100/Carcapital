/**
 * Calendar — moved into `./calendar/`. This file is kept as a barrel so the
 * existing `@/components/shared/week-calendar` import path stays valid.
 * New code should import from `@/components/shared/calendar` directly.
 */
export * from "./calendar";
